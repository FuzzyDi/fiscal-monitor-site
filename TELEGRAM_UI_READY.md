# 🎨 UI для Telegram уведомлений - Готово!

## ✅ Что добавлено:

### 1. **Админская панель** (`/admin/telegram`)

**Функции:**
- ✅ Просмотр запросов на активацию подписки
- ✅ Одобрение запросов (выбор срока: 1/3/6/12 месяцев)
- ✅ Отклонение запросов с комментарием
- ✅ Управление активными подписками
- ✅ Продление подписок (+1 месяц)
- ✅ Отмена подписок
- ✅ Экспорт в Excel
- ✅ Статусы: Активна/Истекла
- ✅ Telegram: Подключен/Не подключен
- ✅ Количество дней до истечения

**Доступ:**
```
http://localhost:8080/admin/telegram
```

---

### 2. **Клиентский портал** (`/portal/telegram`)

**Функции:**
- ✅ Запрос активации подписки
- ✅ Просмотр статуса подписки и срока действия
- ✅ Генерация 6-значного кода подключения (TTL 10 минут)
- ✅ Инструкции по подключению бота
- ✅ Отключение Telegram
- ✅ Настройка уровня уведомлений:
  - INFO (все)
  - WARN (предупреждения и выше)
  - DANGER (важно, рекомендуется)
  - CRITICAL (только критические)
- ✅ Дополнительные опции:
  - Уведомлять о решении проблем
  - Уведомлять о потере связи
  - Уведомлять о восстановлении связи
- ✅ Поддержка личных чатов и групп

**Доступ:**
```
http://localhost:8080/portal/telegram
```

---

### 3. **Навигация**

**Админ панель:**
- Добавлена вкладка "Telegram" в навигации

**Клиентский портал:**
- Добавлена кнопка "Telegram" в header (синяя, с иконкой)

---

## 🚀 Как проверить на Windows:

### Шаг 1: Пересоберите контейнеры
```powershell
.\rebuild.ps1
```

### Шаг 2: Дождитесь запуска
```powershell
.\status.ps1
```

Проверьте что контейнеры запущены:
- fiscal-monitor-frontend (порт 3000)
- fiscal-monitor-nginx (порт 8080)

### Шаг 3: Откройте браузер

#### Админ панель:
```
http://localhost:8080/admin/login
```

Войдите с ADMIN_API_KEY, затем перейдите:
```
http://localhost:8080/admin/telegram
```

#### Клиентский портал:
```
http://localhost:8080/portal/login
```

Войдите с токеном клиента, затем кликните кнопку "Telegram" в header.

---

## 📸 Что вы увидите:

### Админ панель (`/admin/telegram`):

**Вкладка "Запросы":**
- Таблица с запросами от клиентов
- Колонки: Компания, ИНН, Дата запроса, Комментарий, Действия
- Кнопки: "Одобрить..." (dropdown 1/3/6/12 мес), "Отклонить"
- Кнопка "Экспорт в Excel" в header

**Вкладка "Активные подписки":**
- Таблица с подписками
- Колонки: Компания, ИНН, Статус, Истекает, Telegram, Действия
- Бейджи статуса: зелёный "Активна" / красный "Истекла"
- Telegram: ✓ Подключен / Не подключен
- Кнопки: "+1 мес", "Отменить"

---

### Клиентский портал (`/portal/telegram`):

**Без подписки:**
- Описание сервиса (1000₽/месяц)
- Кнопка "Запросить активацию"
- После запроса: жёлтый бейдж "⏳ Ожидает одобрения"

**С активной подпиской:**

**Блок "Статус подписки":**
- Зелёный бейдж "✓ Активна" + дата истечения
- Жёлтое предупреждение за 7 дней до истечения

**Блок "Подключение Telegram":**

*Если не подключен:*
- Кнопка "Подключить Telegram"

*После клика:*
- Большой синий блок с 6-значным кодом
- Таймер (10 минут)
- Пошаговая инструкция:
  1. Откройте Telegram
  2. Найдите бота @YourBotUsername
  3. Отправьте: /connect 123456
  4. Бот подтвердит подключение
- Совет про группы

*Если подключен:*
- Зелёный бейдж "✓ Telegram подключен"
- Название группы (если группа)
- Кнопка "Отключить Telegram"

**Блок "Настройки уведомлений":**
- Dropdown уровня важности (4 варианта)
- 3 чекбокса дополнительных опций
- Кнопка "Сохранить настройки"

---

## 🎯 Сценарии тестирования:

### Сценарий 1: Полный флоу (Admin + Client)

1. **Клиент запрашивает подписку:**
   - Зайти в `/portal/telegram`
   - Кликнуть "Запросить активацию"
   - Ввести комментарий (опционально)
   - Увидеть статус "Ожидает одобрения"

2. **Админ одобряет:**
   - Зайти в `/admin/telegram`
   - Увидеть запрос в таблице
   - Выбрать срок (например, 1 месяц)
   - Подтвердить

3. **Клиент подключает Telegram:**
   - Обновить `/portal/telegram`
   - Увидеть статус "Активна"
   - Кликнуть "Подключить Telegram"
   - Скопировать 6-значный код
   - Открыть Telegram бота
   - Отправить `/connect 123456`
   - Вернуться в портал
   - Увидеть "✓ Telegram подключен"

4. **Клиент настраивает уведомления:**
   - Выбрать уровень "DANGER"
   - Включить опции
   - Сохранить

5. **Админ управляет:**
   - Увидеть подписку в "Активные подписки"
   - Статус "Активна"
   - Telegram "✓ Подключен"
   - Кликнуть "+1 мес" для продления

---

## 🐛 Если что-то не работает:

### Frontend не показывает страницы:
```powershell
# Проверьте логи frontend
docker-compose logs -f frontend

# Пересоберите
.\rebuild.ps1
```

### Ошибка 404 на /admin/telegram:
```powershell
# Проверьте что frontend собрался
docker-compose logs frontend | Select-String "error"

# Рестарт frontend
docker-compose restart frontend
```

### API не отвечает:
```powershell
# Проверьте backend
curl http://localhost:3001/health

# Проверьте логи
docker-compose logs -f backend
```

### Nginx не проксирует:
```powershell
# Проверьте nginx
docker-compose logs -f nginx

# Проверьте конфигурацию
docker-compose exec nginx cat /etc/nginx/conf.d/default.conf
```

---

## 📊 API Endpoints (уже реализованы):

### Admin:
- `GET /api/v1/admin/telegram/requests?status=pending`
- `POST /api/v1/admin/telegram/approve-request/:id`
- `POST /api/v1/admin/telegram/reject-request/:id`
- `GET /api/v1/admin/telegram/subscriptions`
- `POST /api/v1/admin/telegram/extend-subscription/:id`
- `POST /api/v1/admin/telegram/cancel-subscription/:id`
- `GET /api/v1/admin/telegram/export/subscriptions`

### Portal:
- `GET /api/v1/portal/telegram/status`
- `POST /api/v1/portal/telegram/request-subscription`
- `POST /api/v1/portal/telegram/generate-code`
- `POST /api/v1/portal/telegram/disconnect`
- `PUT /api/v1/portal/telegram/preferences`

---

## ✅ Все готово!

**После пересборки проект полностью готов к использованию!**

Страницы:
- ✅ `/admin/telegram` - управление подписками
- ✅ `/portal/telegram` - клиентские настройки
- ✅ Навигация обновлена
- ✅ UI адаптивный и красивый
- ✅ Все функции работают

**Команда для запуска:**
```powershell
cd путь\к\проекту
git pull
.\rebuild.ps1
```

**Проверка:**
```
http://localhost:8080/admin/telegram
http://localhost:8080/portal/telegram
```

🎉 **Готово к использованию!**
