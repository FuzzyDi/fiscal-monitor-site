# Frontend Environment Variables

# API base URL.
#
# 1) Рекомендуемый режим: "unified endpoint" через nginx (и далее — Cloudflare Tunnel).
#    В этом режиме frontend и backend доступны через один хост, а API вызывается
#    относительными путями (/api/v1/...).
#    Оставьте пустым:
# NEXT_PUBLIC_API_URL=

# 2) Локальная разработка БЕЗ nginx (frontend:3000 -> backend:3001):
# NEXT_PUBLIC_API_URL=http://localhost:3001

# 3) Доступ из локальной сети через nginx (LAN):
#    nginx слушает 8080, поэтому используйте IP:8080
# NEXT_PUBLIC_API_URL=http://192.168.1.100:8080

# 4) Доступ через домен в Cloudflare Tunnel (например, fiscaldrive.sbg.network):
#    Также можно оставить пустым (предпочтительно), потому что будет same-origin.
# NEXT_PUBLIC_API_URL=

# ВАЖНО: После изменения этого файла нужно пересобрать frontend (в Docker):
# docker-compose build --no-cache frontend
# docker-compose up -d frontend
