# 📊 Экспорт в Excel - Инструкция

## ✅ Добавлена функция экспорта на страницу State!

### 🎯 Что реализовано

Теперь на странице `/admin/state` есть кнопка **"Экспорт в Excel"**, которая позволяет экспортировать данные о всех кассах в файл Excel.

---

## 📋 Что включает экспорт

### Столбцы в Excel файле:

| Столбец | Описание |
|---------|----------|
| **ИНН** | ИНН компании |
| **Компания** | Название компании из регистрации |
| **Магазин №** | Номер магазина |
| **Касса №** | Номер кассы (POS) |
| **Название** | Название магазина |
| **IP адрес** | IP адрес кассы |
| **Важность** | Уровень важности (INFO, WARN, DANGER, CRITICAL) |
| **Обновлено** | Дата и время последнего обновления |
| **Зарегистрирован** | Да/Нет |
| **Terminal ID** | ID фискального терминала |
| **Чеков** | Количество чеков |
| **Макс. чеков** | Максимальное количество чеков |
| **Не отправлено** | Количество неотправленных чеков |
| **Z-отчётов** | Количество Z-отчётов |
| **Макс. Z** | Максимальное количество Z-отчётов |
| **Осталось Z** | Осталось Z-отчётов до переполнения |
| **Алерты** | Список активных алертов |

### Цветовая маркировка:

- 🔴 **CRITICAL** — красный фон
- 🟠 **DANGER** — оранжевый фон
- 🟡 **WARN** — жёлтый фон
- 🟢 **INFO** — зелёный фон

---

## 🚀 Как использовать

### 1. Откройте страницу State
```
https://fiscaldrive.sbg.network/admin/state
```

### 2. (Опционально) Примените фильтры
- Фильтр по ИНН
- Фильтр по номеру магазина
- Фильтр по уровню важности (severity)

### 3. Нажмите "Экспорт в Excel"
- Кнопка находится справа от заголовка "X POS Terminals"
- При экспорте кнопка покажет "Экспорт..." со спиннером
- Файл автоматически скачается в папку "Загрузки"

### 4. Имя файла
Файл будет называться по шаблону:
```
pos_state_[фильтры]_ГГГГ-ММ-ДД.xlsx
```

Примеры:
- `pos_state_2026-01-27.xlsx` — все кассы
- `pos_state_inn_123456789_2026-01-27.xlsx` — фильтр по ИНН
- `pos_state_shop_1_2026-01-27.xlsx` — фильтр по магазину
- `pos_state_critical_2026-01-27.xlsx` — только критичные

---

## 💻 Как применить изменения на сервере

### Вариант 1: Git Pull + Rebuild (Рекомендуется)
```powershell
cd C:\Projects\fiscal-monitor

# Скачать изменения
git pull origin main

# Пересобрать backend (для добавления endpoint)
docker-compose build --no-cache backend

# Пересобрать frontend (для добавления кнопки)
docker-compose build --no-cache frontend

# Запустить
docker-compose up -d

# Подождать 15 секунд
Start-Sleep -Seconds 15

# Проверить статус
docker-compose ps
```

### Вариант 2: Скачать файлы напрямую
```powershell
cd C:\Projects\fiscal-monitor

# Backend endpoint
Invoke-WebRequest -Uri "https://raw.githubusercontent.com/FuzzyDi/fiscal-monitor-site/main/backend/routes/admin.js" -OutFile "backend/routes/admin.js"

# Frontend API method
Invoke-WebRequest -Uri "https://raw.githubusercontent.com/FuzzyDi/fiscal-monitor-site/main/frontend/lib/api.js" -OutFile "frontend/lib/api.js"

# Frontend page
Invoke-WebRequest -Uri "https://raw.githubusercontent.com/FuzzyDi/fiscal-monitor-site/main/frontend/pages/admin/state.js" -OutFile "frontend/pages/admin/state.js"

# Пересобрать оба сервиса
docker-compose build --no-cache backend frontend
docker-compose up -d
```

---

## ✅ Проверка работоспособности

### 1. Проверить что сервисы запущены
```powershell
docker-compose ps
```

Должны быть **Up** и **healthy**:
- `fiscal-monitor-backend`
- `fiscal-monitor-frontend`

### 2. Проверить логи backend
```powershell
docker-compose logs backend --tail=20
```

Должно быть:
```
✅ Fiscal Monitor API running on 0.0.0.0:3001
```

### 3. Проверить логи frontend
```powershell
docker-compose logs frontend --tail=20
```

Должно быть:
```
✅ ready - started server on 0.0.0.0:3000
```

### 4. Открыть админку
```
https://fiscaldrive.sbg.network/admin/state
```

Должна появиться кнопка **"Экспорт в Excel"** 🎉

---

## 🎨 Как выглядит кнопка

На странице `/admin/state` в синей шапке справа от заголовка "X POS Terminals":

```
┌─────────────────────────────────────────────────────┐
│  📊 X POS Terminals    [📥 Экспорт в Excel] [✕ Clear] │
└─────────────────────────────────────────────────────┘
```

При экспорте:
```
[⏳ Экспорт...]  (с анимацией спиннера)
```

---

## 📝 Технические детали

### Backend Endpoint
```
GET /api/v1/admin/export/state
```

**Query параметры:**
- `shopInn` — фильтр по ИНН (опционально)
- `shopNumber` — фильтр по номеру магазина (опционально)
- `severity` — фильтр по важности (опционально)

**Response:**
- Тип: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- Файл: `.xlsx`

### Frontend API Method
```javascript
adminApi.exportState(key, shopInn, shopNumber, severity)
```

### Используемые библиотеки
- **Backend:** `exceljs` (уже установлен)
- **Frontend:** `axios` (уже установлен)

---

## 🔗 Коммиты

| Коммит | Описание |
|--------|----------|
| `69179d4` | feat: Add Excel export functionality to admin state page |

**GitHub:** https://github.com/FuzzyDi/fiscal-monitor-site

---

## 🎯 Примеры использования

### Пример 1: Экспорт всех касс
1. Откройте: https://fiscaldrive.sbg.network/admin/state
2. Нажмите "Экспорт в Excel"
3. Скачается файл со всеми кассами

### Пример 2: Экспорт только критичных касс
1. Откройте: https://fiscaldrive.sbg.network/admin/state
2. В дашборде нажмите на "Critical: X"
3. На странице state нажмите "Экспорт в Excel"
4. Скачается файл только с критичными кассами

### Пример 3: Экспорт касс конкретной компании
1. Откройте: https://fiscaldrive.sbg.network/admin/dashboard
2. Нажмите на карточку компании (по ИНН)
3. На странице state нажмите "Экспорт в Excel"
4. Скачается файл только для этой компании

---

## ⚠️ Важно!

### Требования
- ✅ Backend должен быть пересобран
- ✅ Frontend должен быть пересобран
- ✅ ExcelJS уже установлен в backend

### Если кнопка не появилась
```powershell
# Полная очистка и пересборка frontend
docker-compose down
docker rmi fiscal-monitor-frontend
docker-compose build --no-cache frontend
docker-compose up -d
```

### Если экспорт не работает (500 ошибка)
```powershell
# Проверить логи backend
docker-compose logs backend --tail=50 | Select-String "export|error"

# Пересобрать backend
docker-compose build --no-cache backend
docker-compose restart backend
```

---

## 🎉 Готово!

Теперь вы можете экспортировать данные о кассах в Excel с фильтрацией по:
- ✅ ИНН компании
- ✅ Номеру магазина
- ✅ Уровню важности

Файл включает:
- ✅ Все данные о кассах
- ✅ Фискальные метрики
- ✅ Алерты
- ✅ Цветовую маркировку по severity

---

**Дата добавления:** 2026-01-27  
**Версия:** 1.0  
**Статус:** ✅ Работает
