# 🎉 Telegram Notifications System - READY!

## ✅ Система полностью разработана и протестирована

---

## 📦 ЧТО БЫЛО СДЕЛАНО

### 1. База данных (8 новых таблиц)
- `notification_subscription_requests` - Запросы клиентов на активацию
- `notification_subscriptions` - Активные подписки
- `notification_preferences` - Настройки клиента
- `telegram_connections` - Подключения Telegram
- `telegram_connect_codes` - Одноразовые коды (TTL 10 мин)
- `notification_cooldowns` - Антиспам механизм
- `notification_queue` - Очередь уведомлений
- `notification_history` - История отправок

### 2. Backend API (12 новых endpoint'ов)

**Для клиентов (Portal):**
- GET /api/v1/portal/telegram/status
- POST /api/v1/portal/telegram/request-subscription
- DELETE /api/v1/portal/telegram/cancel-request
- POST /api/v1/portal/telegram/generate-code
- POST /api/v1/portal/telegram/disconnect
- PUT /api/v1/portal/telegram/preferences

**Для админов:**
- GET /api/v1/admin/telegram/requests
- POST /api/v1/admin/telegram/approve-request/:id
- POST /api/v1/admin/telegram/reject-request/:id
- GET /api/v1/admin/telegram/subscriptions
- POST /api/v1/admin/telegram/extend-subscription/:id
- POST /api/v1/admin/telegram/cancel-subscription/:id
- GET /api/v1/admin/telegram/statistics
- GET /api/v1/admin/telegram/export (Excel!)

### 3. Telegram Bot (5 команд)
- `/start` - Приветствие
- `/connect КОД` - Подключение через 6-значный код
- `/status` - Текущее состояние терминалов
- `/disconnect` - Отключить уведомления
- `/help` - Справка

### 4. Background Worker
- Обработка очереди (каждую минуту)
- Проверка истекающих подписок (ежедневно)
- Очистка старых данных (еженедельно)

### 5. Умная система отправки
- Rate limiting (10 msg/sec по умолчанию, настраивается)
- Группировка алертов (5-минутное окно)
- Cooldown 15 минут на терминал
- CRITICAL отправляются немедленно
- Автоматическая деактивация при блокировке бота

### 6. Экспорт в Excel
- Подписки
- История уведомлений
- Запросы на активацию

### 7. Документация
- **TELEGRAM_CLIENT_GUIDE.md** - Подробная инструкция для клиента
- **TELEGRAM_SETUP.md** - Руководство по настройке для разработчика
- **TELEGRAM_IMPLEMENTATION_SUMMARY.md** - Техническое описание

### 8. Тесты
- Unit тесты всех API endpoint'ов
- Integration тесты
- Security тесты

---

## 🚀 КАК ЗАПУСТИТЬ

### Шаг 1: Создайте Telegram бота

1. Откройте Telegram, найдите @BotFather
2. Отправьте `/newbot`
3. Введите название и username бота
4. Сохраните токен (например: `1234567890:ABCdefGHI...`)
5. Отключите privacy mode: `/setprivacy` → DISABLED

### Шаг 2: Настройте базу данных

```bash
cd /home/user/webapp/backend
psql $DATABASE_URL -f schema.sql
```

Это создаст все необходимые таблицы.

### Шаг 3: Обновите .env

```bash
cd /home/user/webapp/backend
cp .env.example .env
nano .env
```

Добавьте:
```env
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER
TELEGRAM_BOT_USERNAME=ВашБотUsername
TELEGRAM_SEND_DELAY_MS=100
PORTAL_URL=https://fiscaldrive.sbg.network
SUPPORT_EMAIL=support@fiscaldrive.sbg.network
```

### Шаг 4: Установите зависимости

```bash
cd /home/user/webapp/backend
npm install
```

Новые пакеты:
- node-telegram-bot-api
- node-cron
- exceljs
- axios

### Шаг 5: Запустите сервисы

**Вариант А: Отдельные процессы (для разработки)**

```bash
# Терминал 1 - API
cd /home/user/webapp/backend
npm start

# Терминал 2 - Telegram Bot
cd /home/user/webapp/backend
node telegram-bot.js

# Терминал 3 - Background Worker
cd /home/user/webapp/backend
node background-worker.js
```

**Вариант Б: PM2 (для продакшена)**

```bash
cd /home/user/webapp
npm run pm2:start
```

Это запустит все 3 сервиса:
- fiscal-api
- telegram-bot
- notification-worker

Проверить статус:
```bash
npm run pm2:logs
```

### Шаг 6: Проверьте работу

1. **Откройте вашего бота в Telegram:**
   - Найдите по username
   - Отправьте `/start`
   - Бот должен ответить приветствием

2. **Проверьте API:**
   ```bash
   curl http://localhost:3001/health
   ```

3. **Создайте тестовую регистрацию:**
   ```bash
   psql $DATABASE_URL << EOF
   INSERT INTO registrations (shop_inn, title) 
   VALUES ('TEST123', 'Test Company');
   
   INSERT INTO access_tokens (token, shop_inn, label) 
   VALUES ('test-token', 'TEST123', 'Test Token');
   EOF
   ```

4. **Запросите подписку (как клиент):**
   ```bash
   curl -X POST http://localhost:3001/api/v1/portal/telegram/request-subscription \
     -H "Content-Type: application/json" \
     -H "X-Token: test-token" \
     -d '{"comment": "Test"}'
   ```

5. **Одобрите запрос (как админ):**
   ```bash
   curl -X POST http://localhost:3001/api/v1/admin/telegram/approve-request/1 \
     -H "Content-Type: application/json" \
     -H "X-Admin-Key: YOUR_ADMIN_KEY" \
     -d '{"duration_months": 1}'
   ```

6. **Сгенерируйте код:**
   ```bash
   curl -X POST http://localhost:3001/api/v1/portal/telegram/generate-code \
     -H "X-Token: test-token"
   ```

7. **Подключите бота:**
   - Откройте бота в Telegram
   - Напишите: `/connect 847291` (используйте ваш код)
   - Бот ответит: "Уведомления подключены!"

8. **Проверьте статус:**
   ```bash
   /status
   ```

---

## 📚 ДОКУМЕНТАЦИЯ

### Для клиентов
📖 **[TELEGRAM_CLIENT_GUIDE.md](./docs/TELEGRAM_CLIENT_GUIDE.md)**
- Пошаговая инструкция с картинками
- Как запросить активацию
- Как подключить личный чат или группу
- Как настроить уведомления
- FAQ

### Для разработчиков
📖 **[TELEGRAM_SETUP.md](./docs/TELEGRAM_SETUP.md)**
- Создание бота в @BotFather
- Настройка базы данных
- Конфигурация сервисов
- Мониторинг и troubleshooting

### Техническое описание
📖 **[TELEGRAM_IMPLEMENTATION_SUMMARY.md](./docs/TELEGRAM_IMPLEMENTATION_SUMMARY.md)**
- Архитектура системы
- Описание всех компонентов
- Лимиты и оптимизация
- Known issues

---

## 🎯 ОСНОВНЫЕ ФИЧИ

### ✅ Монетизация
- Запрос активации от клиента
- Ручное одобрение админом
- Гибкие периоды подписки
- Отслеживание оплат
- Автопродление (с напоминанием)

### ✅ Безопасность
- Одноразовые 6-значные коды (TTL 10 мин)
- Только одно активное подключение на подписку
- Проверка chat_id (защита от перехвата)
- Rate limiting на /connect (5 попыток за 10 мин)
- Аутентификация клиентов и админов

### ✅ Умные уведомления
- Cooldown 15 минут на один терминал
- Группировка алертов за 5 минут
- CRITICAL отправляются немедленно
- Форматирование: 1 алерт / 2-5 алертов / 5+ алертов (сводка)
- Строгий текстовый стиль без emoji

### ✅ Настройки клиента
- Уровни severity: CRITICAL / DANGER+CRITICAL / WARN+DANGER+CRITICAL
- Уведомления о восстановлении
- Уведомления о потере связи (>15 мин)
- Уведомления о возврате на связь

### ✅ Поддержка групп
- Личные чаты и группы Telegram
- Вся команда видит алерты
- Легко переключаться между чатами

### ✅ Экспорт данных
- Подписки в Excel
- История уведомлений в Excel
- Запросы на активацию в Excel

---

## 🔧 КОНФИГУРАЦИЯ

### Переменные окружения

```env
# Обязательные
TELEGRAM_BOT_TOKEN=<токен от @BotFather>
TELEGRAM_BOT_USERNAME=<username бота>
DATABASE_URL=postgresql://...

# Опциональные (есть defaults)
TELEGRAM_SEND_DELAY_MS=100  # 10 msg/sec
PORTAL_URL=https://fiscaldrive.sbg.network
SUPPORT_EMAIL=support@fiscaldrive.sbg.network
```

### NPM Scripts

```bash
# Установка всех зависимостей
npm run install:all

# Запуск для разработки
npm run dev

# Запуск только Telegram Bot
npm run start:telegram-bot

# Запуск только Worker
npm run start:worker

# PM2 управление
npm run pm2:start     # Запустить все сервисы
npm run pm2:stop      # Остановить
npm run pm2:restart   # Перезапустить
npm run pm2:logs      # Логи

# Тесты
npm test              # Все тесты
cd backend && npm test -- telegram.test.js  # Только Telegram тесты
```

---

## 📊 МОНИТОРИНГ

### Проверка статуса сервисов

```bash
pm2 status
```

Должно быть:
- fiscal-api: online
- telegram-bot: online
- notification-worker: online

### Проверка очереди

```bash
psql $DATABASE_URL -c "
SELECT COUNT(*) FROM notification_queue WHERE processed = false;
"
```

Если > 1000 → проверить worker!

### Проверка активных подписок

```bash
psql $DATABASE_URL -c "
SELECT COUNT(*) FROM notification_subscriptions WHERE status = 'active';
"
```

### Логи

```bash
# Все логи
pm2 logs

# Конкретный сервис
pm2 logs telegram-bot
pm2 logs notification-worker
```

---

## 🐛 TROUBLESHOOTING

### Бот не отвечает
1. Проверить процесс: `pm2 status telegram-bot`
2. Проверить токен в `.env`
3. Проверить privacy mode выключен (в @BotFather)
4. Логи: `pm2 logs telegram-bot --lines 100`

### Уведомления не приходят
1. Проверить worker: `pm2 status notification-worker`
2. Проверить очередь: см. выше
3. Проверить подписку активна
4. Логи: `pm2 logs notification-worker --lines 100`

### Ошибки в БД
1. Проверить подключение: `psql $DATABASE_URL -c "SELECT 1;"`
2. Проверить таблицы: `psql $DATABASE_URL -c "\dt notification*"`
3. Проверить locks: `psql $DATABASE_URL -c "SELECT * FROM pg_locks WHERE NOT granted;"`

---

## ✅ ЧЕКЛИСТ ПЕРЕД ПРОДАКШЕНОМ

- [ ] Telegram бот создан и настроен
- [ ] База данных мигрирована
- [ ] `.env` файл настроен со всеми переменными
- [ ] Зависимости установлены
- [ ] Все 3 сервиса запущены через PM2
- [ ] Тесты пройдены успешно
- [ ] Бот отвечает на `/start`
- [ ] Создан тестовый запрос и одобрен
- [ ] Проверено подключение через код
- [ ] Мониторинг настроен
- [ ] Документация прочитана командой
- [ ] ADMIN_API_KEY изменен на сильный пароль
- [ ] HTTPS включен для PORTAL_URL
- [ ] Backup базы данных настроен

---

## 📞 ПОДДЕРЖКА

Если возникли вопросы:

1. **Проверьте документацию:**
   - [TELEGRAM_CLIENT_GUIDE.md](./docs/TELEGRAM_CLIENT_GUIDE.md) - для клиентов
   - [TELEGRAM_SETUP.md](./docs/TELEGRAM_SETUP.md) - для разработчиков
   - [TELEGRAM_IMPLEMENTATION_SUMMARY.md](./docs/TELEGRAM_IMPLEMENTATION_SUMMARY.md) - техническое

2. **Проверьте логи:**
   ```bash
   pm2 logs
   ```

3. **Проверьте базу данных:**
   ```bash
   psql $DATABASE_URL
   ```

4. **Запустите тесты:**
   ```bash
   npm test
   ```

---

## 🎉 ГОТОВО К ИСПОЛЬЗОВАНИЮ!

Система Telegram уведомлений полностью разработана, протестирована и готова к использованию.

**Основные файлы:**
- `backend/schema.sql` - Схема БД
- `backend/routes/portal-telegram.js` - API для клиентов
- `backend/routes/admin-telegram.js` - API для админов
- `backend/telegram-bot.js` - Telegram бот
- `backend/background-worker.js` - Фоновый обработчик
- `backend/utils/telegram-sender.js` - Отправка сообщений
- `backend/tests/telegram.test.js` - Тесты

**Документация:**
- `docs/TELEGRAM_CLIENT_GUIDE.md` - Подробная инструкция для клиента
- `docs/TELEGRAM_SETUP.md` - Руководство по настройке
- `docs/TELEGRAM_IMPLEMENTATION_SUMMARY.md` - Техническое описание

**Все закоммичено и запушено в GitHub:**
https://github.com/FuzzyDi/fiscal-monitor-site

---

**Создано:** 26 января 2026  
**Версия:** 1.0.0  
**Статус:** ✅ Готово к продакшену

🚀 **Приятного использования!**
