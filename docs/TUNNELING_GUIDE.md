# 🌐 Tunneling Guide - Expose Local Fiscal Monitor to Internet

Это руководство поможет вам **опубликовать локальный сервер в интернет** без деплоя на хостинг.

---

## 📋 Сравнение всех вариантов

| Решение | Цена | Стабильность | Скорость | SSL | Custom Domain | Лимиты |
|---------|------|--------------|----------|-----|---------------|--------|
| **Cloudflare Tunnel** | 🆓 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ✅ Бесплатно | ♾️ Нет |
| **ngrok** | 🆓/$8 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ | 💰 $8/мес | 40 req/мин |
| **Tailscale** | 🆓 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ❌ | Приватная сеть |
| **localtunnel** | 🆓 | ⭐⭐ | ⭐⭐ | ✅ | ❌ | Нестабильно |
| **Serveo** | 🆓 | ⭐⭐ | ⭐⭐⭐ | ✅ | ❌ | Иногда не работает |

---

## 🏆 Cloudflare Tunnel (РЕКОМЕНДУЕТСЯ)

### Почему лучший?
- ✅ **Полностью бесплатно**
- ✅ **Безлимитно** (нет ограничений на трафик)
- ✅ **Быстро** (CDN Cloudflare)
- ✅ **Стабильно** (99.9% uptime)
- ✅ **DDoS защита** бесплатно
- ✅ **Custom domain** бесплатно!
- ✅ **SSL сертификаты** автоматически

---

## 🚀 Быстрый старт с Cloudflare Tunnel

### Вариант 1: Автоматический (через скрипт)

```powershell
cd C:\Projects\fiscal-monitor

# Запустите скрипт
.\setup-cloudflare-tunnel.ps1

# Скрипт сделает всё автоматически:
# 1. Проверит установлен ли cloudflared
# 2. Залогинит вас
# 3. Создаст туннель
# 4. Запустит сервис
```

---

### Вариант 2: Ручная настройка

#### Шаг 1: Установка

**Через winget (рекомендуется):**
```powershell
winget install Cloudflare.cloudflared
```

**Или скачайте вручную:**
1. Перейдите: https://github.com/cloudflare/cloudflared/releases/latest
2. Скачайте `cloudflared-windows-amd64.exe`
3. Переименуйте в `cloudflared.exe`
4. Поместите в `C:\Windows\System32` или добавьте в PATH

#### Шаг 2: Логин

```powershell
cloudflared tunnel login
```

Откроется браузер → залогиньтесь в Cloudflare → разрешите доступ.

#### Шаг 3: Создание туннеля

```powershell
cloudflared tunnel create fiscal-monitor
```

Запомните Tunnel ID (показан в выводе)

#### Шаг 4: Запуск (Quick Tunnel - самый простой)

```powershell
# Просто запустите - получите случайный URL
cloudflared tunnel --url http://localhost:3000
```

**Вывод:**
```
Your quick Tunnel has been created! Visit it at:
https://abc-def-ghi.trycloudflare.com
```

✅ **Готово!** Теперь ваш сайт доступен по этому URL!

⚠️ **Внимание:** URL меняется при каждом перезапуске.

---

### Вариант 3: Постоянный URL с custom domain

Если у вас есть домен (например `fiscalmonitor.uz`):

#### 1. Добавьте домен в Cloudflare

1. Перейдите: https://dash.cloudflare.com
2. Add site → введите `fiscalmonitor.uz`
3. Следуйте инструкциям (измените nameservers у регистратора)

#### 2. Создайте туннель в Dashboard

1. Zero Trust → Networks → Tunnels
2. Create a tunnel → имя: `fiscal-monitor`
3. Install connector → выберите Windows → скопируйте команду
4. Запустите команду в PowerShell

#### 3. Настройте Public Hostname

1. В настройках туннеля → Public Hostname → Add
2. Настройки:
   - **Subdomain:** `app` (или оставьте пустым)
   - **Domain:** `fiscalmonitor.uz`
   - **Type:** HTTP
   - **URL:** `localhost:3000`
3. Save

✅ **Готово!** Теперь доступно по `https://fiscalmonitor.uz` (или `https://app.fiscalmonitor.uz`)

#### 4. Настройте API отдельно (опционально)

Добавьте еще один Public Hostname:
- **Subdomain:** `api`
- **URL:** `localhost:3001`

API будет на `https://api.fiscalmonitor.uz`

---

## 🔧 Конфигурационный файл

Создайте `C:\Users\YOUR_USERNAME\.cloudflared\config.yml`:

```yaml
tunnel: YOUR_TUNNEL_ID
credentials-file: C:\Users\YOUR_USERNAME\.cloudflared\YOUR_TUNNEL_ID.json

ingress:
  # Frontend
  - hostname: fiscalmonitor.uz
    service: http://localhost:3000
  
  # Backend API
  - hostname: api.fiscalmonitor.uz
    service: http://localhost:3001
  
  # Обязательно последним
  - service: http_status:404
```

Запуск:
```powershell
cloudflared tunnel run fiscal-monitor
```

---

## 🤖 Автозапуск как Windows Service

Чтобы туннель запускался автоматически при старте Windows:

```powershell
# Установите как службу
cloudflared service install

# Запустите службу
net start cloudflared

# Проверьте статус
sc query cloudflared
```

Теперь туннель будет работать постоянно, даже после перезагрузки!

---

## 🌐 ngrok (альтернатива)

### Если нужен веб-интерфейс для мониторинга

#### Установка

1. Зарегистрируйтесь: https://ngrok.com/signup
2. Скачайте: https://ngrok.com/download
3. Распакуйте в `C:\ngrok`

#### Настройка

```powershell
# Добавьте authtoken (из dashboard)
ngrok config add-authtoken YOUR_TOKEN
```

#### Запуск

```powershell
# Frontend
ngrok http 3000
```

**Получите URL:** `https://abc123.ngrok.io`

**Веб-интерфейс:** http://127.0.0.1:4040

#### Для 2 портов одновременно (нужен платный $8/мес)

Создайте `ngrok.yml`:
```yaml
version: "2"
authtoken: YOUR_TOKEN
tunnels:
  frontend:
    proto: http
    addr: 3000
  backend:
    proto: http
    addr: 3001
```

Запуск:
```powershell
ngrok start --all --config ngrok.yml
```

---

## 🔒 Tailscale (приватная сеть)

### Когда использовать
- Доступ только для определенных людей
- Безопасный VPN между вашими устройствами

#### Установка

```powershell
winget install tailscale.tailscale
```

#### Настройка

```powershell
# Запустите Tailscale
tailscale up

# Узнайте свой IP
tailscale ip -4
```

#### Доступ

На любом устройстве с установленным Tailscale:
```
http://100.x.x.x:3000
```

#### Поделиться доступом

```powershell
# Сгенерируйте ссылку для приглашения
tailscale share 100.x.x.x:3000
```

---

## ⚡ localtunnel (самый простой)

Если нужно **прямо сейчас** без регистрации:

```powershell
# Установите
npm install -g localtunnel

# Запустите
lt --port 3000

# Получите URL: https://random-word.loca.lt
```

**Минусы:**
- Нестабильно
- Медленно
- Может внезапно отключиться

---

## 🛠️ Настройка Frontend для туннеля

### Важно! API URL должен совпадать

Если ваш туннель: `https://abc.trycloudflare.com`

#### Вариант 1: Один домен для всего

```powershell
cd frontend
echo "NEXT_PUBLIC_API_URL=https://abc.trycloudflare.com" > .env.local
docker-compose restart frontend
```

Настройте Cloudflare для проксирования `/api`:

```yaml
ingress:
  - hostname: abc.trycloudflare.com
    path: ^/api/
    service: http://localhost:3001
  - hostname: abc.trycloudflare.com
    service: http://localhost:3000
```

#### Вариант 2: Разные домены (лучше)

Frontend: `https://app.fiscalmonitor.uz`
Backend: `https://api.fiscalmonitor.uz`

```powershell
cd frontend
echo "NEXT_PUBLIC_API_URL=https://api.fiscalmonitor.uz" > .env.local
docker-compose restart frontend
```

---

## 📊 Мониторинг туннеля

### Cloudflare Dashboard

1. Zero Trust → Analytics
2. Видно:
   - Запросы
   - Ошибки
   - Трафик
   - Uptime

### ngrok Dashboard

1. https://dashboard.ngrok.com
2. Видно:
   - Все запросы в реальном времени
   - Headers
   - Body
   - Response times

### Локальный веб-интерфейс ngrok

```
http://127.0.0.1:4040
```

---

## 🔒 Безопасность

### Cloudflare Access (дополнительная защита)

Ограничьте доступ к админке:

1. Zero Trust → Access → Applications
2. Add application:
   - **Name:** Fiscal Monitor Admin
   - **Domain:** `fiscalmonitor.uz/admin`
3. Add Policy:
   - **Name:** Admins only
   - **Action:** Allow
   - **Rules:** Email is `admin@company.com`

Теперь для доступа к `/admin` нужен email!

### Basic Auth (простой вариант)

Добавьте в nginx перед туннелем:

```nginx
location /admin {
    auth_basic "Admin Area";
    auth_basic_user_file /etc/nginx/.htpasswd;
    proxy_pass http://localhost:3000;
}
```

---

## 🆘 Troubleshooting

### Туннель подключается, но сайт не открывается

**Проверьте:**
```powershell
# Docker контейнеры работают?
docker-compose ps

# Локально работает?
curl http://localhost:3000
```

### CORS ошибки через туннель

**Решение:** Обновите NEXT_PUBLIC_API_URL

```powershell
cd frontend
echo "NEXT_PUBLIC_API_URL=https://your-tunnel-url.com" > .env.local
docker-compose restart frontend
```

### Туннель постоянно отключается

**Cloudflare:**
- Проверьте интернет соединение
- Проверьте не блокирует ли firewall

**ngrok:**
- Free план отключает через 2-8 часов
- Нужен платный для постоянной работы

### Медленно работает

**Причина:** Интернет канал

**Решение:**
- Используйте Cloudflare (CDN кеширует статику)
- Или деплойте на облако

---

## 💡 Рекомендации

### Для демо клиенту:
**Cloudflare Quick Tunnel** - за 1 минуту готово
```powershell
cloudflared tunnel --url http://localhost:3000
```

### Для постоянной работы:
**Cloudflare с custom domain** - надежно и бесплатно

### Для тестирования API:
**ngrok** - удобный веб-интерфейс

### Для приватного доступа команде:
**Tailscale** - безопасный VPN

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте что Docker контейнеры запущены
2. Проверьте что localhost:3000 работает
3. Проверьте логи туннеля
4. Проверьте firewall не блокирует

Удачи! 🚀
